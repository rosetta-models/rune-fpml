namespace fpml.consolidated.business.events : < "fpml definition" >
version "${project.version}"

import fpml.consolidated.*

enum CashOrPhysicalSettlementEnum:
    [docReference ISDA FPML version "recordkeeping-5.13"]

    Cash [ docReference ISDA FPML version "recordkeeping-5.13" ]
    Physical [ docReference ISDA FPML version "recordkeeping-5.13" ]
